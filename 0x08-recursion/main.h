#ifndef MAIN_H
#define MAIN_H
#include<stdio.h>
#include<stdlib.h>
#include<stddef.h>
#include<unistd.h>
int _putchar(char s);
void _puts_recursion(char *s);
void _print_rev_recursion(char *s);
void _int_strlen_recursion(char *s);
int factorial(int n);
int _pow_recusrion(int x, int y);
int sqrt_recusrion(int n);
int is_prime_number(int n);
int is_palindrome(char *s);
int wildcmp(char *s1, char *s2);

#endif
