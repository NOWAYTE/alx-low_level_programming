#include<stdio.h>
/**
 * main -Entry to the main
 * @argv: Argument vector
 * @argc: Argument count
 * @param __attribute__((unused)): Macro
 * Return: Always 0 (success)
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s", argv[0]);
	printf("\n");

	return (0);
}
